tmp/
dist/

# Playwright MCP artifacts
.playwright-mcp/

# Trunk CI/linter cache and configs
.trunk/

# Teacher's own licensed copies of copyright-protected source material (e.g. LISA
# niveaubestimmende Aufgaben). Read by skills as exemplars; never redistributed.
material/

node_modules/

# Per-agent isolated worktrees created by the harness during ce-work.
.claude/worktrees/

# Re-extraction/regeneration output — diffed against the committed file, never
# silently overwritten (KTD3).
*.draft.yaml
